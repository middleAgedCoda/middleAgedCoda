# SHAMWARI Mobile (Expo, lean)

- Start: `npm i && npm start`
- Configure API base: set `EXPO_PUBLIC_API_BASE_URL` in `.env` or use localhost.

Screens:
- Home → CTA to Send
- Send → get quote from server and display breakdown
